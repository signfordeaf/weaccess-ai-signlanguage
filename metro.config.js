// This is a placeholder metro.config.js for the library root
// It's needed when running metro from child directories that watch this folder

module.exports = {
  // Empty config - actual config is in TestApp2/metro.config.js
};
